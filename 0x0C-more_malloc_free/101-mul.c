#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * main - multiplies numbers
 *
 * Return: The multiples of numbers
 */
int main(int argc, char **argv)
{
	int i;
	int mul;

	for (i = 0; i < argc; i++)
	{
		mul *=atoi(argv[i]);
	}
	return (mul);
}
